import { StyleSheet } from "react-native";

export const localStyles = StyleSheet.create({
  subscribeButton: {
    borderRadius: 10,
    overflow: 'hidden',
    marginTop: 30,
    alignItems: 'center',
  },
  gradientButton: {
    padding: 15,
    alignItems: 'center',
  },
  subscribeButtonText: {
    color: '#fff',
    fontSize: 18,
    fontWeight: '600',
  },
});